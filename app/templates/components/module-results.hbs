<li class="module {{if mod.hasRun 'has-run' 'not-run'}}">
  <h2>
    <a href="#" {{action "toggleExpand"}}><span class="arrow {{if isExpanded 'expanded'}}">&#10095;</span></a>
    {{mod.title}}
    <span class="test-count">
      {{#if mod.tests.length}}
        ({{mod.tests.length}})
      {{/if}}
    </span>
  </h2>

  {{#if isExpanded}}
    <ol class="results-tests">
      {{#each mod.tests as |test|}}
        {{test-results test=test class="test"}}
      {{/each}}
    </ol>
  {{/if}}
</li>